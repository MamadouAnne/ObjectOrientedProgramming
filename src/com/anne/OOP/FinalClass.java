package com.anne.OOP;

public class FinalClass {

    // if a class is final no other class extend it.
    // the String class is a final class because strings are immutable
    // we don't use often

    // if a method is declare final it cannot be override
}
